{
  "name": "trading",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dotenv": "^16.0.3",
    "googleapis": "^108.0.1",
    "node-binance-api": "^0.13.1",
    "readline": "^1.3.0",
    "web-vitals": "^2.1.4",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "node server.js",
    "start:assets": "react-scripts start",
    "start:dev": "nodemon --watch src/ --exec ts-node src/server.ts",
    "build:assets": "react-scripts build",
    "build:server": "tsc src/server.ts",
    "build": "react-scripts build && ./node_modules/typescript/bin/tsc",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:money1": "pm2 start binance1.js & pm2 start binance2.js & pm2 start binance3.js & pm2 start binance4.js & pm2 start binance5.js",
    "start:money2": "pm2 start binance6.js & pm2 start binance7.js & pm2 start binance8.js & pm2 start binance9.js & pm2 start binance10.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
